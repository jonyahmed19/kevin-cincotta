import React from "react";
import Painting from "../../components/Painting";

const Sheep = (props) => {
  return (
    <Painting {...props} nickname="Sheep">
    </Painting>
  );
};

export default Sheep;
