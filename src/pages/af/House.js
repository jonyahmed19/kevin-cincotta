import React from "react";
import Painting from "../../components/Painting";

const House = (props) => {
  return (
    <Painting {...props} nickname="House">
    </Painting>
  );
};

export default House;
