import React from "react";
import Painting from "../../components/Painting";

const ParkBench = (props) => {
  return (
    <Painting {...props} nickname="ParkBench">
    </Painting>
  );
};

export default ParkBench;
