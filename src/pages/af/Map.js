import React from "react";
import Painting from "../../components/Painting";

const Map = (props) => {
  return (
    <Painting {...props} nickname="Map">
    </Painting>
  );
};

export default Map;
